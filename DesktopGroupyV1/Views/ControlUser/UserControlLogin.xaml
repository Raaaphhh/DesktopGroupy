<UserControl x:Class="DesktopGroupyV1.Views.ControlUser.UserControlLogin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopGroupyV1.Views.ControlUser"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="1" Grid.Column="1" Text="Connexion" 
                   FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,20"/>

        <!-- Champ Nom d'utilisateur -->
        <TextBlock Grid.Row="2" Grid.Column="1" Text="Nom d'utilisateur" Margin="0,5,0,5"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="2" Grid.Column="1" Margin="0,30,0,5" Height="30"/>

        <!-- Champ Mot de passe -->
        <TextBlock Grid.Row="3" Grid.Column="1" Text="Mot de passe" Margin="0,5,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Grid.Column="1" Margin="0,30,0,5" Height="30"/>

        <!-- Bouton Connexion -->
        <Button Grid.Row="4" Grid.Column="1" Content="Se connecter" 
                HorizontalAlignment="Center" VerticalAlignment="Center" 
                Width="150" Height="40" Click="Login"/>
    </Grid>
</UserControl>
