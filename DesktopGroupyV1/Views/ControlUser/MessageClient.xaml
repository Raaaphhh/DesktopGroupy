<UserControl x:Class="DesktopGroupyV1.Views.ControlUser.MessageClient"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DesktopGroupyV1.Views.ControlUser"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="900" Background="#F5F5F5">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0"
                   Text="Envoyer un message aux clients"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#2D2D2D"
                   Margin="0,0,0,20"/>

        <!-- Sélection du produit -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                Padding="16,12"
                Margin="0,0,0,16">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.08" Color="Black"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Produit ciblé :"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#495057"
                           VerticalAlignment="Center"
                           Margin="0,0,12,0"/>
                <ComboBox x:Name="ListeProduits"
                          ItemsSource="{Binding ProduitsVendeur}"
                          DisplayMemberPath="Nom"
                          Width="220"
                          Height="34"
                          VerticalContentAlignment="Center"
                          SelectionChanged="ListeProduits_SelectionChanged"/>
                <Border Background="#E3F2FD"
                        CornerRadius="12"
                        Padding="10,4"
                        Margin="16,0,0,0"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="NbClientsLabel"
                               Text="0 client(s) concerné(s)"
                               FontSize="13"
                               Foreground="#1565C0"
                               FontWeight="SemiBold"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- Contenu principal : liste clients + composition message -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="1.6*"/>
            </Grid.ColumnDefinitions>

            <!-- Liste des clients concernés -->
            <Border Grid.Column="0"
                    Background="White"
                    CornerRadius="8"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.08" Color="Black"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            Background="#F8F9FA"
                            CornerRadius="8,8,0,0"
                            Padding="16,12"
                            BorderBrush="#E9ECEF"
                            BorderThickness="0,0,0,1">
                        <TextBlock Text="Clients destinataires"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="#495057"/>
                    </Border>

                    <ListBox x:Name="ListeClients"
                             Grid.Row="1"
                             ItemsSource="{Binding ClientsCibles}"
                             Background="Transparent"
                             BorderThickness="0"
                             Margin="8">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}"
                                                    Margin="0,2">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F0F4FF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding NomComplet}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="#2D2D2D"/>
                                    <TextBlock Text="{Binding Email}"
                                               FontSize="12"
                                               Foreground="#868E96"
                                               Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Composition du message -->
            <Border Grid.Column="2"
                    Background="White"
                    CornerRadius="8"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.08" Color="Black"/>
                </Border.Effect>
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Composer le message"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#495057"
                               Margin="0,0,0,16"/>

                    <!-- Objet -->
                    <TextBlock Grid.Row="1"
                               Text="Objet"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="#868E96"
                               Margin="0,0,0,6"/>
                    <TextBox x:Name="ObjetMessage"
                             Grid.Row="2"
                             Height="36"
                             Padding="10,8"
                             FontSize="13"
                             Foreground="#2D2D2D"
                             BorderBrush="#CED4DA"
                             BorderThickness="1"
                             VerticalContentAlignment="Center"
                             Margin="0,0,0,16">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="White"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter Property="BorderBrush" Value="#1565C0"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Corps du message -->
                    <TextBlock Grid.Row="3"
                               Text="Message"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="#868E96"
                               Margin="0,0,0,6"/>
                    <TextBox x:Name="CorpsMessage"
                             Grid.Row="4"
                             Padding="10,8"
                             FontSize="13"
                             Foreground="#2D2D2D"
                             BorderBrush="#CED4DA"
                             BorderThickness="1"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,16">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="White"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter Property="BorderBrush" Value="#1565C0"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Bouton Envoyer -->
                    <Button Grid.Row="5"
                            x:Name="BtnEnvoyer"
                            Content="Envoyer à tous les clients"
                            Height="40"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="EnvoyerMessage">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#1565C0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="8"
                                                    Padding="16,0">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#1976D2"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#0D47A1"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Background" Value="#B0BEC5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
